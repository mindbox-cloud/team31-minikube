apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{ .Release.Name }}
  type: ClusterIP
  ports:
  - name: {{ .Release.Name }}
    protocol: TCP
    port: 8080
    targetPort: 8080